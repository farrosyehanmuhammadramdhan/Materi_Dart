void main() {
  // bool a = true;
  // bool b = false;
  // bool c = true;
  // bool d = false;

  // AND = Beda Nilai True & False = False
  // bool result = a && b;

  // OR = Nilai True / False = True
  // bool result2 = a || b;

  // AND = Nilai True & True = True
  // bool result3 = a && c;

  // AND = Nilai False & False = True
  // bool result4 = b && d;

  // NOT
  // bool a = true;
  // bool b = false;
  // print(!b);

  // print(result);
  // print(result2);
  // print(result3);
  // print(result4);
}
